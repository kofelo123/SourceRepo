## My-Batis MySql

- [Like 연산자](#like-연산자)

```html
WHERE(AND) column LIKE CONCAT('%' , #{search_value},'%')
```
```html
WHERE(AND) column LIKE CONCAT('%' , #{search_value},'%')
```
```html
WHERE(AND) column LIKE CONCAT('%' , #{search_value},'%')
```

### Like 연산자

```html
WHERE(AND) column LIKE CONCAT('%' , #{search_value},'%')
```
